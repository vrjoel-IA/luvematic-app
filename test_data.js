const { createClient } = require('@supabase/supabase-js'); const fs = require('fs'); const dotenv = require('dotenv'); const env = dotenv.parse(fs.readFileSync('.env')); const supabase = createClient(env.VITE_SUPABASE_URL, env.VITE_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.from('Incidencias').select('*, Mantenimientos!inner(id, frecuencia, Instalaciones(id, direccion)), Puertas(id, tipo, id_instalacion), Usuarios!id_tecnico_reparacion(nombre_completo), Checklist_Respuestas(observacion, url_foto)'); if (error) console.log('ERROR:', JSON.stringify(error, null, 2)); else console.log('SUCCESS:', data.length); } test();  
