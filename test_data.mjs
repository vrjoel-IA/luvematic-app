import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import dotenv from 'dotenv'; const env = dotenv.parse(fs.readFileSync('.env.local')); const supabase = createClient(env.VITE_SUPABASE_URL, env.VITE_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.from('Incidencias').select('*, Mantenimientos!inner(id, frecuencia, Instalaciones(id, direccion)), Puertas(id, tipo, id_instalacion), Usuarios!id_tecnico_reparacion(nombre_completo), Checklist_Respuestas(observacion, url_foto)'); console.log('ERROR:', error); console.log('DATA LENGTH:', data?.length); } test();  
